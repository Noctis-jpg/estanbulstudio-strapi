'use strict';

/**
 * fixed-whatsapp-btn router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::fixed-whatsapp-btn.fixed-whatsapp-btn');
