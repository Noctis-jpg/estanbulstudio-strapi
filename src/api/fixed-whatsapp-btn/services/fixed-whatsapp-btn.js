'use strict';

/**
 * fixed-whatsapp-btn service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::fixed-whatsapp-btn.fixed-whatsapp-btn');
